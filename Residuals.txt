set logscale y
set title "Residuals"
set ylabel 'Residual'
set xlabel 'Iteration'
set key outside
set grid


plot "<cat log.txt | cut -d' ' -f2" with linespoints

pause 1
xmax = GPVAL_DATA_X_MAX+100
xmin = xmax-600
set xrange [xmin:xmax]
reread
